version: '3.8'

services:
  base:
    image: edufolly/work-with-secrets:${VERSION:-CHECK_VERSION_IN_BRANCH_PROD}
    secrets:
      - my-secret
    deploy:
      mode: replicated
      replicas: 1

secrets:
  my-secret:
    external: true
